/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.env
.env.local
.DS_Store

# Local Claude Code tooling (skills, commands, agents) — not part of the product
/.claude
# impeccable critique snapshots — local tooling output
/.impeccable
